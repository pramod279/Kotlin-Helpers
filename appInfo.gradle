/**
 * This Gradle Contains App Version Related Information
 */
def app_info = [:]

/**
 * Application ID
 */
app_info.applicationID = "com.pramod.kotlinhelpers"

/**
 * Debug Build Version Information
 */
app_info.debug_version_code = 1
app_info.debug_version_name = "1.0"

/**
 * QA Build Version Information
 */
app_info.qa_version_code = 1
app_info.qa_version_name = "1.0"

/**
 * Release Build Version Information
 */
app_info.release_version_code = 1
app_info.release_version_name = "1.0"

ext.app_info = app_info

/**
 * Build Version Information
 */
def build_version = [:]
/**
 * Minimum & Maximum Supported Versions for the App
 */
build_version.min_sdk = 21
build_version.target_sdk = 28

ext.build_version = build_version